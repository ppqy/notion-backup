NODE_ENV=production
HOST=0.0.0.0
PORT=3000
DATA_DIR=/data
DATABASE_PATH=/data/app.db
BACKUP_ROOT=/data/backups
# Optional. If omitted, the app generates one under DATA_DIR and shows it once during setup.
# APP_ENCRYPTION_KEY=
SESSION_COOKIE_NAME=notion_backup_session
SESSION_SECURE=false
